# Project Examples

This folder separates the current VisiumHD example from older historical examples.

## Main example to use now

Use:

```text
examples/current_visium_hd_exp1/
```

This is the current main project example:

```text
H&E + official FICTURE paired candidate crop
-> candidate mask pool
-> component-aware union
-> Test1 and Test2 VLM evaluation
```

## Legacy examples

Older examples are kept under `examples/legacy_examples/` for backward compatibility
with old scripts:

- `legacy_examples/tma24/example1.jpg`: older TMA24 / silicosis H&E example.
- `legacy_examples/spatiallibd/example2.png`: older spatialLIBD / prompt-inference example.
- `legacy_examples/kvasir_seg/Kvasir-SEG/`: Kvasir-SEG smoke-test data.

They are not the current main VisiumHD storyline.
